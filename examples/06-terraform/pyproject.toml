[project]
name = "06-terraform"
version = "0.1.0"
description = "MCP server Terraform example"
requires-python  = ">=3.10"
dependencies = [
    "mcp[cli]",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-anyio",
]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]