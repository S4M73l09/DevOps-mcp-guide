[project]
name = "mcp-example-02-first-tool"
version = "0.1.0"
description = "MCP server with a first tool"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-anyio",
]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]
