[project]
name = "mcp-example-04-validation"
version = "0.1.0"
description = "MCP server with Validation"
requires-python  = ">=3.10"
dependencies = [
    "mcp[cli]",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-anyio",
]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]