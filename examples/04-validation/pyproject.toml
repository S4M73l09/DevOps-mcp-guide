[project]
name = "mcp-example-04-validation"
version = "0.1.0"
description = "MCP server with static and dynamic resources"
requires-python  = ">=3.10"
dependencies = [
    "mcp[cli]",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-anyio",
]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["."]